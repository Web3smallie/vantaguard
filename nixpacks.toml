[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip3 install -r agent/requirements.txt --break-system-packages"]

[start]
cmd = "python3 /app/agent/agent.py"